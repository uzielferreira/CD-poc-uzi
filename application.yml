apiVersion: argoproj.io/vlalpha1
kind: Application
metadata:
  name: site-poc-uzi
  namespace: uzi-space
spec:
  destination:
    namespace: default
    server: https://kubernetes.default.svc
  project: default
  source:
    path: CD-poc-uzi/Kubernetes
    repoURL: https://github.com/uzielferreira/CD-poc-uzi
    targetRevision: HEAD
  syncPolicy:   
    syncOptions:
      - CreateNameSpace=true
    automated:
      selfHeal: true
      prune: true